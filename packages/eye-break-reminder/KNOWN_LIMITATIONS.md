# 已知限制

- 当前候选版仅面向 Windows x64。
- 应用提供基础系统托盘，但不提供开机自启、声音提醒或跨设备同步。
- 托盘菜单仅用于显示主窗口、恢复关闭时询问和退出应用，不直接控制提醒状态。
- 应用重启后始终处于暂停状态，不会自动恢复上次运行中的计时。
- 便携版未使用商业代码签名证书，Windows 可能显示未知发布者或 SmartScreen 提示。
- 本轮自动化验证覆盖了暂停、锁定、托盘恢复和退出流程，但没有把真实硬件休眠/锁屏作为已完成测试。
- 本工具只提供休息提醒，不构成医疗建议。
