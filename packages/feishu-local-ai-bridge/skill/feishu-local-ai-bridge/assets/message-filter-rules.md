# 消息过滤规则

按以下顺序处理，任一失败即停止：

1. 验证事件签名、时间窗和应用身份；
2. 检查事件编号是否已经处理；
3. 只接受配置允许的聊天类型；
4. 只接受显式允许的发送者；
5. 只处理指定命令前缀；
6. 限制文本长度、附件类型和单次资源数量；
7. 解析权限等级和明确写意图；
8. 将路径规范化为工作区内相对路径；
9. 读取Git基线并识别用户已有修改；
10. 执行后回读结果，生成最小回包。

拒绝回包只说明必要原因，例如“未授权”“只读模式”“目标不在允许工作区”或“重复事件”。不要回显规则明细、允许用户列表、凭据名称或主机路径。
