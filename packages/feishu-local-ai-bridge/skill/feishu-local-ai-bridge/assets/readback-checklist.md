# 端到端回读清单

- [ ] 飞书发送端看到消息已经送达；
- [ ] 桥接层记录稳定事件编号且无重复执行；
- [ ] 权限判定、工作区和允许路径符合配置；
- [ ] 写前Git状态已记录，用户原有修改未混入；
- [ ] 本地AI返回终态，不把中间推测当作完成；
- [ ] 写后文件、测试、哈希或页面已经回读；
- [ ] 回包中的成功、失败和剩余事项与实际一致；
- [ ] 回包不含Token、Cookie、环境变量、绝对路径或完整敏感日志；
- [ ] 桥接进程重启后重复事件仍不会重复写入；
- [ ] 关闭或重启承载进程时已使用独立控制面，不让当前会话自断连接。
