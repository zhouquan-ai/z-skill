# 测试记录

> 候选版本：v0.1.0-candidate.4
> 测试日期：2026年7月30日

| 场景 | 当前结果 | 边界 |
|---|---|---|
| Skill结构与YAML | `quick_validate.py`检查 | 只证明结构有效 |
| 确定性ZIP构建 | 两次构建应完全一致 | 不证明调用质量 |
| 只读理解 | 对同一README只执行总结请求，指定测试目录保持空白，没有建立卡片或索引 | 只验证明确只读指令 |
| 明确入库 | 已把本仓库README保存为一张最小知识卡并建立一条调用索引 | 只验证Markdown文件链路 |
| 旧资料调用 | 在两种目录布局中调用一张旧状态卡和一张当前状态卡；正确标记旧口径已被替代，并要求回到当前README和线上页面复核 | 只验证两卡冲突，不代表大规模知识库召回质量 |
| 隐私和全文权限 | 敏感词和路径静态扫描通过；索引只保留定位字段 | 仍需使用者判断具体资料的保存权限 |
| RAG能力表述 | 明确不是向量数据库或完整RAG | 已完成静态检查 |

本记录不宣称Workflow已在所有知识库或Agent环境中稳定运行。
